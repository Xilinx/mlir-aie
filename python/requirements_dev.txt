cmake>=3.31, <4.0
pybind11>=3.0.4
setuptools>=61.0
wheel
ninja!=1.13.0
cibuildwheel
pre-commit
# black[jupyter] so the format check covers .ipynb code cells too
# (plain `black` skips notebooks silently, leaving stale formatting in
# whats_new_in_unify_compilation_workflow.ipynb etc.).
black[jupyter]
nanobind>=2.9
lit
matplotlib
# lit requires psutil to set timeouts
psutil
pytest
pyyaml
cloudpickle # required by eudsl when it is vendored instead of installed
